version: '3.8'

services:
  nginx:
    image: nginx:alpine
    container_name: pokemon-game
    ports:
      - "82:80"
    volumes:
      - ./:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
